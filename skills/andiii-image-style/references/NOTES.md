# NOTES — 引擎建设决策记录

## Phase 0 决策（2026-08-03）

### 主力后端：Wan2.7-Image（内置 image_generate）✅ 定案
- 中文标题渲染**实测通过**（"雨天旧书店"五字清晰准确，无错字乱码）
- 水彩质感实测到位（纸纹/晕染/笔触全部呈现）
- 封面走**图内文字**方案（不用排版层叠文字备选）

### 备选后端：Agnes（agnes-image-2.1-flash）
- ⚠️ 直连 `apihub.agnes-ai.com` **超时**（WinError 10060，墙外 API）
- 需要走 CLIProxyAPI 中转（Docker，TCP 转发绕 Clash）才能测
- 暂缓，非必需

### 视觉质检：小米 MiMo v2.5（auxiliary.vision）
- provider=xiaomi, model=mimo-v2.5（Hermes `_PROVIDER_VISION_MODELS` 写死此模型）
- **坑**：`.env` 必须用官方 base_url `https://api.xiaomimimo.com/v1`（OpenAI 兼容）
- **坑**：Token Plan 的 `tp-` key 与 sk key 不能混用；base_url 指向 token-plan-cn 时 sk key 会 401
- **坑**：Hermes 实际读 `D:\tools\hermes\.env`（不是 `~/.hermes/.env`）；改 env 后需重启应用才生效（进程缓存）
- 视觉可用后，出图复查可部分自动化（但仍以用户确认为准）

### 锚点图
- 待补充：用户已确认的对味封面 2-3 张 → `assets/anchors/`（每张配描述 md）

## 引擎 v1.0 实测记录（2026-08-03）

- ✅ Wan2.7 中文标题 ≤10 字渲染可靠（"雨天旧书店"两次验证无错字）
- ✅ 17 字长句金句卡两行排布完整渲染、绝对居中（超出预期）
- ✅ 质检门 3 组样例：雷区 FAIL / 引擎四段式 PASS / 残缺 FAIL
- ⚠️ 1:1 标题垂直位置 Wan2.7 无法稳定居中（偏上/略偏下）→ 规则已放宽为 25%-60% 可接受，硬性要求：标题完整 + 左右留边 ≥15%（用户拍板）
- ✅ 封面 16:9 首图一次达标（标题水平居中、垂直 40-60%）
- ✅ 端到端实测完成（2026-08-03）：《雨天，去旧书店坐一坐》全链路走通，5 图自动上传，草稿推送成功（media_id 已返回）
- ⚠️ 封面格式坑：PIL 存 JPEG 内容但命名 .png → wechat-api 报 "Format mismatch"（功能正常但难看）→ 规范：封面/配图统一 .jpg 扩展名

## 引擎 v1.1 修复记录（2026-08-03 深度自查）

- 🔴 **封面裁剪切主体（实测确认）**：中心裁切切主体、底部锚点切标题 → 正解=**安全区设计**：封面 prompt 附加「主体+标题集中垂直 25%-75% 中央带、上下 15% 纯纸留白」约束句 → 中心裁剪双保（已实测验证标题+主体完整）。新增 `scripts/crop_image.py`（center/bottom 锚点 + JPEG 压缩）
- ⚠️ **图内标题 vs 平台标题双轨**：图内文字必须 = 平台标题短版或意象词，禁止自创；交付说明列「图内文字/平台标题」供核对
- ✅ **视觉复核自动化**：出图 → MiMo 固定问句硬检查（错字/留边/被裁/雷区）→ FAIL 重生成一次 → PASS 才交用户最终确认；视觉只做硬检查，审美以用户为准
- ✅ **金句不重复**：金句卡句子 ≠ 正文引用块/结尾金句（同一句只出现一次）；只有一句金句时出纯意象卡
- ✅ 中文 stdin 管道实测 PASS + 脚本加 UTF-8 reconfigure 防御（Windows GBK 兜底）
- ✅ 弹药库 10/10 PASS（修复后回归）

## v1.2 修正（2026-08-03 用户纠正：过度约束）

- **引擎退回守风格底线**：只管配色体系/纸纹质感/雷区规避；构图与隐喻完全自由
- **封面默认纯意象无文字**（用户确认：封面没必要放文字，标题由平台显示）；图内文字改为可选规则，仅用户要求时启用
- 双尺寸协议安全区改宽松版（只要求上下边缘纯纸留白，不规定坐标数字）
- 弹药库 10 模板 P1 布局参数全部放开（"主体位置与构图自由，保持留白呼吸感"）
- 视觉复核检查项适配（标题检查仅当图内文字时）
- 教训：工程约束（为裁剪服务的坐标）≠ 创作约束；用户要的是"手绘水彩的味道"不是"元素坐标"

## v1.2.1 复查修复（2026-08-03 第二轮自查）

- 清除 5 处 21:9 残留（frontmatter description / 四段式模板 P1 / 输出格式 / 软规避清单）
- 脚本 HARD_BLOCK 补中文雷区词（炭黑金/高饱和/渐变紫/动漫脸/卡通）+ **"避免/不要/no/avoid/without" 豁免逻辑**（防误杀"避免高饱和"类正常表述，实测通过）
- 视觉复核固定问句模板化（纯意象/图内文字/金句卡三套问句，禁止自由发挥问句）
- 弹药库 P3 文字槽位标注"默认无文字"（防内文配图被塞字）
- README 修正产物入库说明（outputs 不入库是设计）
- v1.2 纯意象封面实测：主体完整+无雷区+水彩质感达标；顶部窗框略紧 → 靠"裁后必复核"兜底（安全区约束句只提首过率，复核是真正防线）

## 引擎版本
- v1.0.0：四件套 + 四段式模板 + 弹药库 10 类 + 质检门脚本 + 双尺寸 + 金句卡
- v1.1.0：安全区设计 + 视觉复核自动化 + 标题双轨规则 + 金句不重复 + crop_image.py
- v1.2.0：去约束化 — 封面默认无文字、构图全自由、宽松安全区、弹药库布局放开
- v1.3.0（风格库化，2026-08-03）：多引擎 + 路由

## v1.4 设计推理层（2026-08-04 调研落地）

- **调研收获**（回答"如何确保艺术感/设计感"）：AI 生图"像 AI 图"的根源是**没有设计意图**（只有怎么画、没有为什么这么画）→ 模型只能平均化风格
- 借鉴：design-image-studio（设计编译器三层 design_reasoning→brief→prompt + anti-slop 抢救手册）、Anthropic 官方 canvas-design（设计哲学先行、运动命名、用户输入不限制创作自由）、brand-guidelines（品牌视觉系统）
- 落地：`references/design-reasoning.md`（6 项设计推理模板 + 艺术指导词表 + anti-slop 清单 + 单变量迭代）——**全部 5 引擎工作流第 0 步必做**（先定「为什么这么画」再编译）
- 安全记录：第三方 skill 生态 36% 含 prompt injection（Snyk ToxicSkills 研究）——只吸收理念不装第三方；design-image-studio anti-slop 全文存档 `references/learnings/`
- 遗留：Andiii 品牌视觉基准（借鉴 brand-guidelines 建品牌色板/字体基线，可选）

## 风格库化记录（v1.3，2026-08-03）

- **用户纠正 1**：配图风格应按文风匹配，不是默认全用水彩 → 建 `references/image-style-routing.md`（文风→风格→引擎路由）
- **用户纠正 2**：风格是参考不是限制——激发创意，不锁死发挥；允许跨风格混搭/偏离（原则已写入全部引擎第零节 + 路由表总原则）
- 引擎家族（5 个，全部过质检门 + 同步 Hermes）：
  1. `andiii-image-style` 水彩（弹药库 10/10 PASS）
  2. `andiii-zine-style` Zine 拼贴（适配 muted-zine-poster-v01，上游署名保留；实测出图：印刷颗粒/拼贴质感，主体完整无雷区 ✅）
  3. `andiii-heytear-style` 喜茶新中式（子 agent 蒸馏，6/6 PASS）
  4. `andiii-sketchy-style` 潦草手账（子 agent 蒸馏，6/6 PASS）
  5. `andiii-minimal-style` 石墨极简（自建，5/5 PASS）
- 质检门词表扩至 5 系（水彩/zine/喜茶/潦草/极简）+ 纹理计数排除"避免X"豁免
- 教程/清单类信息图不走 AI 生图（文字乱码）→ gzh-design 组件/HTML/SVG
- zine 引擎暂无弹药库（四段式+9 项变体引擎足够），后续按需补
- **新引擎实测出图通过（2026-08-03）**：zine（印刷颗粒/拼贴）、喜茶（宣纸/水墨/茶渍东方感）、潦草（手账涂鸦/胶带/马克笔）——三系风格差异化验证，均主体完整无雷区无文字；测试图存桌面「风格库测试图/」
